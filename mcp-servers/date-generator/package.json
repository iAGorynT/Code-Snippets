{
  "name": "date-generator-mcp",
  "version": "4.0.9",
  "description": "MCP server for generating dates by month, year, and day of week",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "test": "tsx test.ts",
    "dev": "tsx --watch src/index.ts",
    "version": "test -f manifest.json && jq '.version = env.npm_package_version' manifest.json > tmp.json && mv tmp.json manifest.json || true"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "tsx": "^4.22.3",
    "typescript": "^6.0.3"
  },
  "keywords": [
    "mcp",
    "dates",
    "calendar"
  ],
  "author": "Bruce Guthrie",
  "license": "MIT"
}
